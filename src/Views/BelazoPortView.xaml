<UserControl x:Class="Chezar.Views.BelazoPortView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Chezar.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <DockPanel LastChildFill="True" Grid.Row="0">
            <TextBlock Text="Ключ" Margin="5" />
            <TextBox x:Name="Key" Margin="5"  DockPanel.Dock="Right"/>    
        </DockPanel>
        <TextBlock x:Name="InputTextEntropy" Grid.Row="1"/>
        <TextBox x:Name="InputText" TextWrapping="Wrap" Margin="10" Grid.Row="2" />
        <TextBlock x:Name="OutputTextEntropy" Grid.Row="3"/>
        <TextBlock x:Name="OutputText" TextWrapping="Wrap" Margin="10" Grid.Row="4" />
    </Grid>
</UserControl>